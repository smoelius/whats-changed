[package]
name = "whats-changed"
version = "0.2.0"
description = "Show Rust dependencies that were upgraded"
edition = "2024"
license = "MIT OR Apache-2.0"
repository = "https://github.com/smoelius/whats-changed"

[dependencies]
anyhow = "1.0"
elaborate = "1.1"
semver = "1.0"
toml = "1.1"

[dev-dependencies]
assert_cmd = "2.2"
tempfile = "3.27"
