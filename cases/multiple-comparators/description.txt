Tests that whats-changed gracefully reports an error on stderr when a
dependency's version requirement contains more than one comparator (e.g.,
">=1.0, <2.0"), which minimum_version_for_req does not support. Since this
is the manifest's only dependency, no section heading is printed to stdout.
