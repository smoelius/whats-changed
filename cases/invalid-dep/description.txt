Tests that whats-changed gracefully reports an error on stderr, rather than
aborting, when a dependency has no recognizable version specifier (no
"version", "git", "path", or "workspace" key). Since this is the manifest's
only dependency, no section heading is printed to stdout.
