Tests that whats-changed gracefully reports an error on stderr when a
dependency uses a version operator (tilde) that minimum_version_for_req does
not support. Since this is the manifest's only dependency, no section
heading is printed to stdout.
